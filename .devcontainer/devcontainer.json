{
    // 1. Имя контейнера
    "name": "Go Project (Go 1.25.4)",

    // 2. Использование базового образа
    // Мы используем официальный, минималистичный образ Debian Bookworm.
    // Это наш "чистый холст".
    "image": "mcr.microsoft.com/devcontainers/base:debian-12",

    // 3. Использование "Features" для добавления Go и других инструментов
    // Dev Container Features - это переиспользуемые скрипты установки.
    "features": {
        // Добавляем поддержку Go с конкретной версией (например, 1.25.4).
        // Обратите внимание, что версия 1.25.* является экспериментальной/будущей на данный момент 
        // и может потребовать уточнения актуального номера версии на момент использования.
        // Используйте последнюю стабильную версию, если 1.25.* еще не выпущена.
        "ghcr.io/devcontainers/features/go:1": {
            "version": "1.25.4" 
        },
        // Пример добавления другой фичи, например, Docker CLI внутри контейнера
        "ghcr.io/devcontainers/features/docker-in-docker:1": {
            "version": "latest"
        }
    },

    // 4. Настройка VS Code
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go" // Обязательное расширение VS Code для работы с Go
            ]
        }
    },

    // 5. Команды после создания контейнера
    // Например, загрузка зависимостей проекта
    "postCreateCommand": "go mod download"
}