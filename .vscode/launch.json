{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${file}"
        },
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",

            "preLaunchTask": "build" 
        }
    ],
    "inputs": [
        {
            "id": "appName",
            "type": "promptString",
            "description": "Enter the name for the output executable file:",
            "default": "myprogram"
        }
    ]

}
